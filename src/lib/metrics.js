export function summarize(rows=[]){const sum=(k)=>rows.reduce((a,x)=>a+Number(x[k]||0),0);const views=sum("views"),clicks=sum("clicks"),sales=sum("sales"),revenue=sum("revenue"),commission=sum("commission");return{views,clicks,sales,revenue,commission,ctr:views?clicks/views*100:0,conversion:clicks?sales/clicks*100:0}}export function decision(m){if(!m||m.views<300)return"TESTAR";if(m.sales>0||m.clicks/Math.max(m.views,1)>.02)return"REPETIR";return"PARAR"}