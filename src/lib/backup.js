export function downloadBackup(data){const blob=new Blob([JSON.stringify({version:1,createdAt:new Date().toISOString(),data},null,2)],{type:"application/json"});const a=document.createElement("a");a.href=URL.createObjectURL(blob);a.download="autoonn-backup.json";a.click();URL.revokeObjectURL(a.href)}export async function readBackup(file){const x=JSON.parse(await file.text());if(x?.version!==1||!x?.data)throw new Error("Backup incompatível");return x.data}