export function validateProduct(p){const errors=[];if(!p?.name?.trim())errors.push("Nome obrigatório");if(Number(p?.price)<0)errors.push("Preço inválido");if(Number(p?.commission)<0)errors.push("Comissão inválida");return{ok:errors.length===0,errors}}export function normalizeProduct(p){return{...p,name:String(p.name||"").trim(),price:Number(p.price||0),commission:Number(p.commission||0),demand:Math.max(0,Math.min(100,Number(p.demand??50))),proof:Math.max(0,Math.min(100,Number(p.proof??50))),ease:Math.max(0,Math.min(100,Number(p.ease??50)))}}