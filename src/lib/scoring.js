export function productScore(p){const c=Math.min(Number(p.commission||0)/20,1)*35;const demand=Math.min(Number(p.demand||50)/100,1)*30;const proof=Math.min(Number(p.proof||50)/100,1)*20;const ease=Math.min(Number(p.ease||50)/100,1)*15;return Math.round(c+demand+proof+ease)}export function performanceScore(m){const views=Math.max(Number(m.views||0),1);const retention=Math.min(Number(m.retention||0),100);const ctr=Math.min(Number(m.clicks||0)/views*100,10)*10;const conv=Math.min(Number(m.sales||0)/Math.max(Number(m.clicks||0),1)*100,20)*5;return Math.round(retention*.4+ctr*.3+conv*.3)}